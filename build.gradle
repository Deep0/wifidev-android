buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // 推荐使用 Android Gradle 插件最新稳定版，例如 8.0.2（根据实际情况调整）
        classpath 'com.android.tools.build:gradle:8.0.2'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
